# The local backend is hosted on the LAN server. Override this per environment.
VITE_API_URL=http://192.168.1.60:8080/api/v1/additions
# Required when the backend has ADD2NUM_API_KEY configured.
# Do not put a real value in this example file.
VITE_API_TOKEN=<backend-api-key>
