# Scripts

The project uses package scripts as the cross-platform verification entry points:

- `npm test` runs the unit test harness.
- `npm run build` type-checks and bundles the app.
- `npm run verify` runs both checks.
